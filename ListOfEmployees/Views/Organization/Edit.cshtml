@model ListOfEmployees.ViewModels.OrganizationVM

@{
    ViewData["Title"] = "Редактировать Организацю";
}

<h3>Редактировать</h3>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="NameId" />
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group btnMarginTop">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
                <a asp-action="Delete" asp-route-id="@Model.NameId" class="btn btn-danger delete">Удалить</a>
                <a asp-action="Index" class="btn btngray">Отмена</a>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        $(function ()  /*Подверждение удаление */ {
            $("a.delete").click(function () {
                if (!confirm("Вы уверены что хотите удалить?")) return false;
            });
        });
    </script>
}